{{- if .Values.sts.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "fakes3pp.fullnameSTS" . }}
  labels:
    {{- include "fakes3pp.labelsSTS" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "fakes3pp.fullnameSTS" . }}
  minReplicas: {{ .Values.sts.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.sts.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.sts.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.sts.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.sts.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.sts.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
